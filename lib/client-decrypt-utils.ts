"use client"

// Función para mostrar el número de cuenta completo
export function displayAccountNumber(account: string): string {
  if (!account) return "No disponible"
  
  // Mostrar el número de cuenta completo
  return account
}
